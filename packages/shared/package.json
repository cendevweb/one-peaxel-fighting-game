{
  "name": "@opfg/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Network protocol, validation schemas and constants shared by the web client and the game server.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "build": "tsc -b", "clean": "rm -rf dist *.tsbuildinfo" },
  "dependencies": {
    "@opfg/combat-core": "*",
    "zod": "^4.6.5"
  }
}
