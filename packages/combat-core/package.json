{
  "name": "@opfg/combat-core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, renderer-agnostic fighting game simulation. Runs in Node and in the browser.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
