{
  "name": "@opfg/server",
  "version": "0.1.0",
  "private": true,
  "description": "Authoritative realtime game server: rooms, matchmaking and the shared combat simulation.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@opfg/combat-core": "*",
    "@opfg/shared": "*",
    "socket.io": "^4.8.3"
  }
}
